the Divide and Conquer approach is more efficient and uses a recursive method to split the set of points into 1/2, based on their x-coordinates. The time complexity of this algorithm is O(n log n), making it faster than brute force. 

--------------------------------------------------------------------------------------------------------------------------------
How to Run)

To see the outputfile. 
1) Run Make
2) ./P1
3) Display the output


4) Make Clean to remove executables
